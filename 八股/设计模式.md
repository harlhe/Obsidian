## 单例模式
保证一个类只有一个实例，并提供一个全局访问点来获取这个实例。
**多种实现方式**：能手写出懒汉式、饿汉式、静态内部类、枚举这四种实现
进阶：
### 懒汉式
### 饿汉式
### 静态内部类
### 枚举
### 如何防止反射和序列化破坏单例？
### 不同实现方式的优缺点是什么？

## 工厂模式
### 简单工厂
### 工厂方法
### 抽象工厂
### 框架中的应用
Spring 的 `BeanFactory` 和 `ApplicationContext`、JDK 中的 `Calendar.getInstance()`、`Integer.valueOf()` 等都是典型的工厂模式应用。
## 代理模式
### 静态代理 vs 动态代理
### JDK 动态代理
### CGLIB 动态代理
### 框架中的应用：Spring AOP 的实现原理就是动态代理

## 策略模式
## 其他模式
- **建造者模式 (Builder Pattern)**： 通过链式调用分步骤地创建属性繁多的复杂对象，让构建过程更清晰。
    
- **装饰器模式 (Decorator Pattern)**： 像套娃一样，在不改变原对象结构的情况下，动态地为对象添加新功能。
    
- **适配器模式 (Adapter Pattern)**： 如同一个转换插头，让两个原本接口不兼容的类可以协同工作。
    
- **观察者模式 (Observer Pattern)**： 定义了一种“一对多”的依赖关系，当被观察者状态改变时，所有观察者都会自动收到通知。
    
- **模板方法模式 (Template Method Pattern)**： 在父类中定义好算法的骨架和流程，并将其中可变的步骤延迟到子类中实现。

## 代理模式和适配器模式有什么区别？

- **目的不同** ：代理模式主要关注控制对对象的访问，而适配器模式则用于接口转换，使不兼容的类能够一起工作。
- **结构不同** ：代理模式一般包含抽象主题、真实主题和代理三个角色，适配器模式包含目标接口、适配器和被适配者三个角色。
- **应用场景不同** ：代理模式常用于添加额外功能或控制对对象的访问，适配器模式常用于让不兼容的接口协同工作。
## volatile和sychronized如何实现单例模式

```javascript
public class SingleTon {

    // volatile 关键字修饰变量 防止指令重排序
    private static volatile SingleTon instance = null;
    private SingleTon(){}
     
    public static  SingleTon getInstance(){
        if(instance == null){
            //同步代码块 只有在第一次获取对象的时候会执行到 ，第二次及以后访问时 instance变量均非null故不会往下执行了 直接返回啦
            synchronized(SingleTon.class){
                if(instance == null){
                    instance = new SingleTon();
                }
            }
        }
        return instance;
    }
}
```

正确的双重检查锁定模式需要需要使用 volatile。volatile主要包含两个功能。

- 保证可见性。使用 volatile 定义的变量，将会保证对所有线程的可见性。
- 禁止指令重排序优化。

由于 volatile 禁止对象创建时指令之间重排序，所以其他线程不会访问到一个未初始化的对象，从而保证安全性。
